# Місія №2: Діагностика ALU

Агенте, вітаю. Термінал активовано (Місія №1).

Твоя нова задача — провести діагностику "серця" машини — її Арифметико-Логічного Пристрою (ALU). Ми маємо переконатися, що він коректно виконує базові операції ADD (додавання) та SUB (віднімання).

## Ваше Завдання

### 1. Модифікуй arithmetic.asm

* Відкрий файл `arithmetic.asm` у редакторі.
* Знайди всі коментарі, що містять `ЗАВДАННЯ` та `???`.
* Заповни пропуски, керуючись інструкціями в коментарях.

**Мета:**

* Частина 1: `EAX = [first_num] + [second_num]` (25 + 17)
* Частина 2: `EDX = [first_num] - [second_num]` (25 - 17)

### 2. Скомпілюй

* Відкрий термінал у цій директорії.
* Виконай команду `make`.
* Якщо все правильно, ти побачиш файл `arithmetic` (без розширення).

### 3. Перевір

Виконай кроки з "Протоколу діагностики" нижче.

## Протокол Діагностики (GDB)

**Проблема:** Наша програма _не_ друкує результат на екран. У нас ще немає "драйвера" для виводу чисел.

**Рішення:** Ми використаємо діагностичний зонд `gdb` (GNU Debugger), щоб "зазирнути" всередину процесора і подивитися на значення в регістрах.

### Крок 1: Запуск зонда

У терміналі введи:

```bash
gdb ./arithmetic
```

Ти потрапиш у середовище gdb, воно виглядає як `(gdb)`.

### Крок 2: Встановлення "ін'єкції" (Breakpoint)

Ми кажемо програмі зупинитися на самому початку.

```gdb
(gdb) b _start
```

GDB підтвердить: `Breakpoint 1 at 0x...`

### Крок 3: Запуск програми

```gdb
(gdb) run
```

Програма запуститься і миттєво зупиниться на `_start`.

### Крок 4: Перевірка Додавання (EAX)

Тепер ми виконуємо програму крок за кроком. Команда `ni` (next instruction) виконує одну інструкцію.

1. Введи `ni` та натисни Enter. (Виконається `mov eax, [first_num]`)
2. Введи `ni` та натисни Enter. (Виконається `mov ebx, [second_num]`)
3. Введи `ni` та натисни Enter. (Виконається `add eax, ebx`)

Тепер обчислення 25 + 17 завершено. Результат має бути в EAX. Перевіримо:

```gdb
(gdb) info registers eax
```

**Очікуваний результат:** Ти маєш побачити щось на кшталт `eax 0x2a 42`.  
Це означає, що EAX містить 42 (або 0x2a у шістнадцятковій системі).

### Крок 5: Перевірка Віднімання (EDX)

Продовжуємо виконувати кроки:

1. Введи `ni` та натисни Enter. (Виконається `mov edx, [first_num]`)
2. Введи `ni` та натисни Enter. (Виконається `sub edx, [second_num]`)

Обчислення 25 - 17 завершено. Результат в EDX. Перевіримо:

```gdb
(gdb) info registers edx
```

**Очікуваний результат:** Ти маєш побачити `edx 0x8 8`.  
Це означає, що EDX містить 8.

### Крок 6: Завершення

Діагностику завершено. Виходимо з gdb:

```gdb
(gdb) quit
```

## Звіт

**Доказ виконання:** Зроби скріншоти свого термінала, де чітко видно:

1. Виконання `make`.
2. Запуск `gdb`.
3. Результат команди `info registers eax` (де видно 0x2a / 42).
4. Результат команди `info registers edx` (де видно 0x8 / 8).

Додай ці скріншоти до свого звіту для Google Classroom.
